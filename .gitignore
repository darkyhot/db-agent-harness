__pycache__/
*.pyc
.ipynb_checkpoints/
.env
.venv/
.pytest_cache/
.codex

# Memory — ignore DB files, keep directory
memory/*
!memory/.gitkeep

# Workspace — ignore runtime files, keep directory
workspace/*
!workspace/.gitkeep

# Data — ignore CSV/JSON data files, keep directory
# data_for_agent/*
# !data_for_agent/.gitkeep

# Embeddings cache (строится автоматически, не коммитится)
data_for_agent/embeddings.npz
